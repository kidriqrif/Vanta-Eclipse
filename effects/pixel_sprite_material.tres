[gd_resource type="ShaderMaterial" load_steps=2 format=3]

[ext_resource type="Shader" path="res://effects/pixel_sprite.gdshader" id="1"]

[resource]
; Local to scene so each user owns its own copy: enemy_view.gd retints
; rim_color per enemy from EnemyDefinition.glow_color, and that must not
; leak into any other screen sharing this material.
resource_local_to_scene = true
shader = ExtResource("1")
